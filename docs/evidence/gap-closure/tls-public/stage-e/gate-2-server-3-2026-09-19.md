# Gate 2 — `veridex-server-3` enforcement rollout

| | |
|---|---|
| **Verdict** | **PASS** |
| **Transition anchor (UTC)** | `2026-09-19T15:28:54.7806606Z` |
| **60-minute checkpoint (UTC)** | `2026-09-19T16:31:20.9970610Z` |
| **Formal final gate (UTC)** | `2026-09-19T16:43:20.4543749Z` |
| **Observed duration** | 60.29 minutes at the acceptance checkpoint; final gate later |
| **Target** | `veridex-server-3`; private `10.2.1.12`; public `152.53.142.131` |
| **Runtime target** | pod `cilium-7tnsw`; container `73dc519b1285f`; host endpoint `4053` |

## Preconditions and recovery

- A separately named, non-disruptive K3s etcd snapshot existed before the
  transition:
  `stage-e-server3-pre-enforcement-20260919-veridex-server-3-1789831191`.
- Snapshot size was `22,671,392` bytes and its SHA-256 was
  `08cb50a68336a2a3f3d9931874b3bf64d9078640d774be70707aa8387d519e1a`.
- Root SSH and an authenticated Netcup SCP Screen console were held open.
- The console independently resolved Cilium container `73dc519b1285f` and
  host endpoint `4053`, then proved `PolicyAuditMode: Enabled` before the
  transition.
- The console rollback was prepared as:

  ```text
  crictl exec 73dc519b1285f cilium-dbg endpoint config 4053 PolicyAuditMode=Enabled
  ```

## Order decision

The live preflight found `veridex-server-2` was both the etcd leader and the
kube-vip lease holder. `veridex-server-3`, which held neither role, was
therefore promoted first. `veridex-server-1` remains last because the local
Kubernetes tunnel terminates there.

## Baseline and transition

Before the transition:

- all five nodes were Ready;
- all nine Argo CD applications were Synced/Healthy;
- API readiness, etcd peer traffic, and the private control-plane path were
  healthy;
- Cilium on server-3 reported `OK`;
- endpoint `4053` reported `PolicyAuditMode: Enabled`;
- a clean five-minute monitor sample showed etcd TCP `2380` from both peer
  control-plane nodes allowed;
- unsupported-IPv6 neighbor-discovery noise and unsolicited world scans were
  classified separately from the internal stop condition.

After explicit endpoint-specific approval, the only state change was:

```text
cilium-dbg endpoint config 4053 PolicyAuditMode=Disabled
```

The command returned `Endpoint 4053 configuration updated successfully`, and
an immediate readback reported `PolicyAuditMode: Disabled`. No policy
manifest was applied or reverted.

## Observation and final gate

The health watcher completed its required window at 60.29 minutes. Every
recorded checkpoint reported:

- zero non-Ready nodes;
- zero unhealthy or OutOfSync applications;
- API readiness `ok`; and
- endpoint `4053` still `PolicyAuditMode: Disabled`.

The verdict stream continued to allow legitimate internal traffic, including
etcd TCP `2380` and VXLAN UDP `8472`. Policy denials were unsolicited public
internet traffic, not sources in `10.2.0.0/16`, `10.44.0.0/16`, or
`10.45.0.0/16`. No internal stop condition occurred.

At the formal final gate:

- all five nodes were Ready;
- all nine applications were Synced/Healthy;
- `/readyz?verbose` passed, including `etcd` and `etcd-readiness`;
- kube-vip remained held by `veridex-server-2`;
- Cilium on server-3 reported `OK`;
- the versioned drift checker dynamically resolved all five current endpoint
  IDs and found every endpoint in sync with the staged desired-state file;
- TCP `443`, `32537`, and `32538` passed on both worker public IPs.

## Result

`veridex-server-3` passed and remains in enforcement mode. No rollback was
performed. Stage E is not yet complete: `veridex-server-2` and then
`veridex-server-1` still require separately approved transitions and clean
60-minute observation windows, followed by the final cluster-wide gate.
